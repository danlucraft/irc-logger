#!/usr/bin/env ruby
# encoding: binary

$:.push(File.expand_path("../../lib", __FILE__))
require 'irc_logger'

server = ARGV[0]
channel = ARGV[1]
username = ARGV[2]
target = ARGV[3]
p [server, channel, username, target]
bot = IrcLogger::Logbot.new(server, channel, username, target)
bot.connect